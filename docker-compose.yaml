services:
  mongo:
    image: mongo:latest
    ports:
      - '27017:27017'
    volumes:
      - mongo_data:/data/db
    networks:
      - devops-network
  app:
    build:
      context: .
    ports:
      - '4000:3000'
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - devops-network
    depends_on:
      - mongo
    restart: unless-stopped

volumes:
  mongo_data:
    driver: local

networks:
  devops-network:
    driver: bridge